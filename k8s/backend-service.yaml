apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      name: http-metrics
      port: 8000
      targetPort: 8000
  type: LoadBalancer
